stages:
  - test

test:
  stage: test
  image: python:3.8
  script:
    - pip install -r requirements.txt
    - pip install -r requirements-dev.txt
    - pip install -e .
    - python -m pytest tests/ -v
